你现在在我的 VS Code 工作区 `c:\workspace\tmp\interview-prep` 里继续工作。

目标：
1. 从 `notes/嵌入式面试宝典.pdf` 中检索“xxxx”这道题的答案（优先提取原书要点，不要泛泛而谈）。（我已经将嵌入式面试宝典.pdf的文字内容提取到嵌入式面试宝典.txt当中，你可以先检查一下）
2. 把答案补充到 `notes/c_cpp/关键字.md` 末尾 `#### 什么情况下使用const关键字？` 下面。
3. 风格必须和前文一致：中文问答笔记风格、分点清晰、必要时给简短代码示例、不要突兀换风格。

已知环境：
- Windows

执行要求：
- 先抽取 PDF 文本并定位相关段落，再编辑 markdown。
- 直接改文件，不只给建议。
- 完成后汇报：改了哪些内容、对应文件路径。