# 📓 每日学习日志

## 日期：2026年03月31日（星期二）

---

### ✅ 今日完成

#### 主线（C/C++）
- [ ] 

#### 支线（OS/网络/驱动/AI）
- [ ] 

#### 刷题
- [x] LeetCode [1002]：[查找共用字符]
  思路：用哈希表统计每个单词中每个字母出现的次数，然后对每个字母在所有单词中取出现次数的最小值，最小值是几就说明这个字母在所有单词中公共出现了几次，最后按最小值把对应字母加入结果数组返回。
  代码位置：`code/leetcode/2026-03-31-find-common-characters.c`

#### 项目/其他
- [ ] 

---

### 💡 知识点记录 / 重点摘录

---

### ❓ 遇到的问题 / 未解决的疑问
```c
// ❌ 错误：**words 是 char 类型，不能与 NULL 比较
if (**words == NULL || wordsSize == 0)

// ✅ 修复：检查指针本身
if (words == NULL || wordsSize == 0)
```

```c
// ❌ 错误：变长数组不支持初始化列表
int hashmap[wordsSize][26] = {0};

// ✅ 修复：改用 memset
int hashmap[wordsSize][26];
memset(hashmap, 0, sizeof(hashmap));
```
---