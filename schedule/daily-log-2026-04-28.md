# 📓 每日学习日志

## 日期：2026年04月28日（星期二）

---

### ✅ 今日完成

#### 主线（C/C++）
- [ ] 

#### 支线（OS/网络/驱动/AI）
- [ ] 

#### 刷题
- [x] LeetCode [692]：[前K个高频单词]
  思路：第一步：统计每个单词出现频次 第二步：先按字典序排序 第三步：再按频次排序（稳定性保证字典序） 第四步：取前k个返回。
  代码位置：`code/leetcode/2026-04-28-top-k-frequent-words.c`

- [x] LeetCode [520]：[检测大写字母]
  思路：统计大写字母数量 cnt： cnt == len → 全部大写 ✅ cnt == 0 → 全部小写 ✅ cnt == 1 且 首字母大写 → 只有首字母大写 ✅ 其他情况 → false ❌。
  代码位置：`code/leetcode/2026-04-28-detect-capital.c`

- [x] LeetCode [14]：[最长公共前缀]
  思路：以第一个字符串为基准： 逐列比较每个字符串的同一位置字符 发现不匹配 → 截断返回 全部匹配 → 继续下一列。
  代码位置：`code/leetcode/2026-04-28-longest-common-prefix.c`

- [x] LeetCode [521]：[最长特殊序列 Ⅰ]
  思路：情况1：两个字符串相同 任何一个的子序列也是另一个的子序列 不存在特殊序列 → 返回 -1 情况2：两个字符串不同 较长的字符串本身就是最长特殊序列 → 返回较长字符串的长度。
  代码位置：`code/leetcode/2026-04-28-longest-uncommon-subsequence-i.c`

- [x] LeetCode [522]：[最长特殊序列 II]
  思路：对每个字符串： 检查它是否是其他所有字符串的子序列 如果都不是 → 它就是特殊序列 记录最长的特殊序列长度。
  代码位置：`code/leetcode/2026-04-28-longest-uncommon-subsequence-ii.c`
#### 项目/其他
- [ ] 

---

### 💡 知识点记录 / 重点摘录

---

### ❓ 遇到的问题 / 未解决的疑问
- 问题一：编译报错 - 比较函数类型不匹配
	```c
	/* ❌ 错误写法 */
	int cmp1(const node *a, const node *b)

	/* ✅ 正确写法 */
	int cmp1(const void *a, const void *b)
	{
		const node *na = (const node *)a;  // 内部转换
		const node *nb = (const node *)b;
	}
	```
- 问题二：逻辑错误 - 频次统计位置错误
	```c
	/* ❌ 错误写法：统计到了错误位置 */
	if (strcmp(node_w[j].word, words[i]) == 0) {
		node_w[node_len].count++;  // node_len是新位置，不是j
	}

	/* ✅ 正确写法 */
	if (strcmp(node_w[j].word, words[i]) == 0) {
		node_w[j].count++;  // 应该是j
	}
	```
- 问题三：运行时错误 - 缓冲区溢出
	```c
	/* ❌ 错误写法 */
	char word[10];  // 只能存9个字符，放不下'\0'

	/* ✅ 正确写法 */
	char word[11];  // 存10个字符 + '\0'
	```
---